//- Ads slot — dispatches to the active ad network configured in site.js (site.adProvider).
//-  Default network is EthicalAds; set adProvider: 'carbon' to use Carbon Ads.
if site.adProvider === 'carbon'
  include carbon-ads
else
  include ethical-ads
